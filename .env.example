# Required
MONGODB_URI=mongodb://user:password@host:port/database

# Optional (defaults shown)
PORT=3000
DB_NAME=trunk_recorder
COLLECTION_NAME=radio_events
CAPTURE_DURATION=15000  # Duration in ms for capture.js utility
